import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['localhost', '34.47.71.46', 'tistory', 'blogspot', 'googlesyndication.com'],
  },
};

export default nextConfig;
